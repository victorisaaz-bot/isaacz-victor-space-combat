/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // Set to false to avoid double mounting in dev for requestAnimationFrame audio/canvas
};

export default nextConfig;
